# AGENTS.md — ポートフォリオサイトのレビュー手順

このサイトは [Quartz](https://quartz.jzhao.xyz/) で構築された静的サイトです。
コンテンツはすべて `content/` 以下の Markdown ファイルです。

---

## ファイル構成

```
content/
  成果物一覧.md          # メインの成果物ページ（正）
  flutterの成果物一覧.md  # Flutter 成果物（成果物一覧.md から言語で抜粋）
  CSharpの成果物一覧.md   # C# 成果物（同上）
  products/              # 各成果物の詳細ページ
  assets/                # 画像・動画
```

---

## レビューの流れ

### 1. 言語別ページの整合性チェック

`成果物一覧.md` が正となるファイルです。
言語別ページ（`flutterの成果物一覧.md` / `CSharpの成果物一覧.md`）は成果物一覧.md の内容を言語でフィルタしたものです。

成果物一覧.md にエントリを追加・更新したら、該当する言語別ページも同様に更新されているか確認してください。

**対応表:**

| タグ | 更新対象ページ |
|------|--------------|
| `#Flutter` | `flutterの成果物一覧.md` |
| `#CSharp` / `#ASPNET` | `CSharpの成果物一覧.md` |

**チェック項目:**
- 成果物一覧.md に追加されたエントリが言語別ページにも存在するか
- 本文・箇条書きの内容が一致しているか（特に詳細化・修正された箇所）
- タグが一致しているか（`#ハッカソン` など後から追加されるものに注意）
- 見出しレベルが言語別ページの規則に合っているか（言語別ページは `##` が年、`###` がプロジェクト名）

### 2. ページタイトルチェック

Quartz はページタイトルを以下の優先度で決定します: `frontmatter title` > `H1見出し` > ファイル名スラグ

**チェック項目:**
- 各ページに `# タイトル` または `frontmatter: title:` が設定されているか
- タイトルがページの内容と一致しているか（例: `# はじめに` は成果物一覧ページとして意味が通るか）
- `products/` 以下のファイルにタイトルがない場合、ファイル名がそのままタイトルになる点に注意

**既知の状態:**
- `content/成果物一覧.md`: H1 が `# はじめに`（ファイル名と異なる）
- `content/aspnetMVCUserTokenTest.md`: H1 が `# 概要` のみで本文なし
- `content/products/会話サポートサービス「Ally」（GDGoC Japan Hackathon）.md`: H1 なし（ファイル名がタイトルになる）

### 3. wikiリンクの整合性チェック

Quartz の wikiリンク `[[ファイル名]]` はファイル名のステム（拡張子なし）で解決されます。
サブフォルダ（`products/`）内のファイルも同様に解決されます。

**チェック項目:**
- `[[リンク先]]` に対応するファイルが `content/` または `content/products/` に存在するか
- ファイルをリネームしたとき、そのファイルへのすべての wikiリンクも更新されているか

確認コマンド:
```bash
# すべての wikiリンクを列挙
grep -rh '\[\[' content/ --include="*.md" | grep -o '\[\[[^\]]*\]\]' | sort -u

# リンク先ファイルの存在確認（例: gdgoc-hackathon）
find content/ -name "gdgoc-hackathon.md"
```

### 4. 未完成・空コンテンツのチェック

**チェック項目:**
- ファイルが空でないか（`wc -c` や `cat` で確認）
- 文章が途中で途切れていないか（「- 主な機能に」など末尾が不完全な箇条書き）
- `[[詳細はこちら]]` などのリンクの直前に、そのリンク先に対応するプロジェクト説明があるか

```bash
# 空ファイルの確認
find content/ -name "*.md" -empty
```

### 5. 基本的な確認事項

- **誤字・脱字**: 特に技術用語（「アーキテクチャに依存しない」など）
- **見出しレベルの整合性**: h4 (`####`) が h3 (`###`) より前に来ていないか
- **ファイル末尾の改行**: Quartz は改行なしでも動作するが、git diff を見やすくするために末尾改行を推奨
- **画像パスの存在確認**: `![[/assets/...]]` のパスに実際にファイルがあるか

```bash
# 画像参照の一覧
grep -rh '!\[\[' content/ --include="*.md" | grep -o '/assets/[^]]*' | sort -u
```

---

## 現在の既知の問題

| ファイル | 問題 |
|---------|------|
| `content/成果物一覧.md:19` | `[[gdgoc-hackathon]]` のリンク先ファイルが存在しない（`会話サポートサービス「Ally」（GDGoC Japan Hackathon）.md` に改名済みのため） |
| `content/インターン経験.md` | ファイルが空 |
| `content/aspnetMVCUserTokenTest.md` | H1「概要」のみで本文なし |
